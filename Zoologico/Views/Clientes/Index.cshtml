@model IEnumerable<Zoologico.Models.Cliente>

@{
    ViewBag.Title = "Index";
    Layout = "~/Views/Shared/_LayoutTrabajadores.cshtml";
}

<link href="~/Library/style/Workers.css" rel="stylesheet" />
<br />
<h2 class="text-center">Clientes Registrados</h2>


<a href="@Url.Action("Create", "Clientes")" class="btn btn-success mao">
    <i class="icon-pencil icon-white"></i>
    <span>
        <strong>Registrar Cliente</strong>
    </span>
</a>

<table class="table">
    <tr>
        <th>
            Cedula
        </th>
        <th>
            Nombre
        </th>
        <th>
            Apellido
        </th>
        <th>
            Direccion
        </th>
        <th>
            Telefono
        </th>
        <th>
            Edad
        </th>
        <th>
            Password
        </th>
        <th></th>
    </tr>

    @foreach (var item in Model)
    {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Cedula_Cliente)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Nombre_Cliente)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Apellido_Cliente)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Direccion_Cliente)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Telefono_Cliente)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Edad_Cliente)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Pass_Cliente)
            </td>


            <td>
                @Html.ActionLink("Editar", "Edit", new { id = item.Cedula_Cliente }, new { @class = "btn btn-success" })
                @Html.ActionLink("Eliminar", "Delete", new { id = item.Cedula_Cliente }, new { @class = "btn btn-danger" })
                @Html.ActionLink("Detalle", "Details", new { id = item.Cedula_Cliente }, new { @class = "btn btn-success" })
            </td>


        </tr>
    }

</table>
