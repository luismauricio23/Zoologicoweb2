@model IEnumerable<Zoologico.Models.Veterinario>

@{
    ViewBag.Title = "Index";
    Layout = "~/Views/Shared/_LayoutTrabajadores.cshtml";
}
    <link href="~/Library/style/Workers.css" rel="stylesheet" />
<br />
<h2 class="text-center">Veterinarios</h2>

<a href="@Url.Action("Create", "Veterinarios")" class="btn btn-success mao">
    <i class="icon-pencil icon-white"></i>
    <span>
        <strong>Nuevo Veterinario</strong>
    </span>
</a>
<table class="table">
    <tr>
        <th></th>
        <th>
            Cedula
        </th>
        <th>
            Nombre
        </th>
        <th>
            Apellido
        </th>
        <th>
            Telefono
        </th>
        <th>
            Direccion
        </th>

        <th></th>
    </tr>

    @foreach (var item in Model)
    {
<tr>
    <td></td>
    <td>
        @Html.DisplayFor(modelItem => item.Cedula_Veterinario)
    </td>
    <td>
        @Html.DisplayFor(modelItem => item.Nombre_Veterinario)
    </td>
    <td>
        @Html.DisplayFor(modelItem => item.Apellido_Veterinario)
    </td>
    <td>
        @Html.DisplayFor(modelItem => item.Telefono_Veterinario)
    </td>
    <td>
        @Html.DisplayFor(modelItem => item.Direccion_Veterinario)
    </td>
    <td>
        @Html.ActionLink("Editar", "Edit", new { id = item.Cedula_Veterinario }, new { @class = "btn btn-success" })
        @Html.ActionLink("Eliminar", "Delete", new { id = item.Cedula_Veterinario }, new { @class = "btn btn-danger" })
        @Html.ActionLink("Detalle", "Details", new { id = item.Cedula_Veterinario }, new { @class = "btn btn-success" })
    </td>
</tr>
    }

</table>
