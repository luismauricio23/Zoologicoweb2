@model IEnumerable<Zoologico.Models.Veterinario>

@{
    ViewBag.Title = "Index";
    Layout = "~/Views/Shared/_LayoutTrabajadores.cshtml";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Nombre_Veterinario)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Apellido_Veterinario)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Telefono_Veterinario)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Direccion_Veterinario)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Nombre_Veterinario)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Apellido_Veterinario)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Telefono_Veterinario)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Direccion_Veterinario)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.Cedula_Veterinario }) |
            @Html.ActionLink("Details", "Details", new { id=item.Cedula_Veterinario }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.Cedula_Veterinario })
        </td>
    </tr>
}

</table>
